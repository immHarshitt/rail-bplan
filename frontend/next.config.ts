import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the dev-mode overlay indicator so the demo UI is unobstructed.
  devIndicators: false,
};

export default nextConfig;
